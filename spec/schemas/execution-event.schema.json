{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://operon.dev/spec/0.2/execution-event.schema.json",
  "title": "Operon execution event",
  "oneOf": [
    {
      "type": "object",
      "properties": {
        "kind": { "const": "session_loaded" },
        "protocol_version": { "const": "0.2" },
        "request_id": { "type": "integer", "minimum": 1 },
        "artifacts": { "type": "array", "items": { "type": "object" } }
      },
      "required": ["kind", "protocol_version", "request_id", "artifacts"],
      "additionalProperties": false
    },
    {
      "type": "object",
      "properties": {
        "kind": { "const": "generation_completed" },
        "protocol_version": { "const": "0.2" },
        "request_id": { "type": "integer", "minimum": 1 },
        "response": { "type": "object" }
      },
      "required": ["kind", "protocol_version", "request_id", "response"],
      "additionalProperties": false
    },
    {
      "type": "object",
      "properties": {
        "kind": { "const": "retrieval_completed" },
        "protocol_version": { "const": "0.2" },
        "request_id": { "type": "integer", "minimum": 1 },
        "sources": { "type": "array", "items": { "type": "object" } }
      },
      "required": ["kind", "protocol_version", "request_id", "sources"],
      "additionalProperties": false
    },
    {
      "type": "object",
      "properties": {
        "kind": { "const": "memory_search_completed" },
        "protocol_version": { "const": "0.2" },
        "request_id": { "type": "integer", "minimum": 1 },
        "records": { "type": "array", "items": { "type": "object" } }
      },
      "required": ["kind", "protocol_version", "request_id", "records"],
      "additionalProperties": false
    },
    {
      "type": "object",
      "properties": {
        "kind": { "const": "output_validated" },
        "protocol_version": { "const": "0.2" },
        "request_id": { "type": "integer", "minimum": 1 },
        "errors": { "type": "array", "items": { "type": "string" } }
      },
      "required": ["kind", "protocol_version", "request_id", "errors"],
      "additionalProperties": false
    },
    {
      "type": "object",
      "properties": {
        "kind": { "const": "skill_completed" },
        "protocol_version": { "const": "0.2" },
        "request_id": { "type": "integer", "minimum": 1 },
        "result": { "type": "object" }
      },
      "required": ["kind", "protocol_version", "request_id", "result"],
      "additionalProperties": false
    },
    {
      "type": "object",
      "properties": {
        "kind": { "const": "skill_prepared" },
        "protocol_version": { "const": "0.2" },
        "request_id": { "type": "integer", "minimum": 1 },
        "outcome": {
          "type": "object",
          "properties": {
            "kind": { "enum": ["ready", "needs_input", "rejected", "unavailable"] }
          },
          "required": ["kind"]
        }
      },
      "required": ["kind", "protocol_version", "request_id", "outcome"],
      "additionalProperties": false
    },
    {
      "type": "object",
      "properties": {
        "kind": { "const": "command_failed" },
        "protocol_version": { "const": "0.2" },
        "request_id": { "type": "integer", "minimum": 1 },
        "failure": {
          "enum": ["provider", "grounding", "memory", "session", "skill", "cancelled", "timeout"]
        },
        "message": { "type": "string" }
      },
      "required": ["kind", "protocol_version", "request_id", "failure", "message"],
      "additionalProperties": false
    }
  ]
}
